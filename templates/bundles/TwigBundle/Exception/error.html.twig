{% extends 'layout_white.html.twig' %}
{% set PAGE_ID = 'error' %}
{% set PAGE_ID = 'article_adjustment_of_valves_and_gbo_kaput' %}

{% block title %}
    <title>Автомагистр - ошибка {{ status_code }}</title>
{% endblock %}

{% block content %}
    <main class="site-main error-page">
        <div class="error-page__body">
            <div class="error-page__back">
                {{ status_code }}
            </div>
            <div class="error-page__front">
                <div class="container">
                    <h1 class="error-page__title">
                        {% block error_massage %}
                            К сожалению, произошла ошибка. <br>
                            Мы уже работаем над решением проблемы.
                        {% endblock %}
                    </h1>
                    <div class="error-page__note">
                        Но вам, возможно, будут интересны следующие страницы:
                    </div>
                    <nav class="error-page__nav">
                        <a class="btn btn_shmatn error-page__btn"
                           href="{{ path('service') }}">
                            <i class="icon-gear"></i>
                            АВТОСЕРВИС В МОСКВЕ
                        </a>
                        {#
                        <a class="btn btn_shmatn error-page__btn" href="{{ path('shop_index') }}">
                            <i class="icon-belt"></i>
                            МАГАЗИН ЗАПЧАСТЕЙ
                            </a>
                        #}
                    </nav>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
