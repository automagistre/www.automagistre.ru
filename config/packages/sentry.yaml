parameters:
    env(SENTRY_DSN): null
    env(SENTRY_DSN_BROWSER): null

sentry:
    dsn: "%env(SENTRY_DSN)%"
    options:
        environment: '%kernel.environment%'
        excluded_exceptions:
            - 'Symfony\Component\HttpKernel\Exception\HttpExceptionInterface'
            - 'Symfony\Component\Security\Core\Exception\AccessDeniedException'
